import 'package:flutter/cupertino.dart';

final greenColor = Color(0xFF63B5AF); // Green color in ARGB format
final darkGreenColor = Color(0xFF0C4C48); // Dark Green color in ARGB format

final grayColor = Color(0xFF808080); // Gray color in ARGB format
final lightGrayColor = Color(0xFFD3D3D3); // Light Gray color in ARGB format
final darkGrayColor = Color(0xFFA9A9A9); // Dark Gray color in ARGB format

final redColor = Color(0xFFE83559); // Red color in ARGB format
final darkRedColor = Color(0xFFE70F0F); // Dark Red color in ARGB format

final blackColor = Color(0xFF000000); // Black color in ARGB format
final whiteColor = Color(0xFFFFFFFF); // White color in ARGB format

// Gradients
final buttonGradient = [Color(0xFF63B5AF), Color(0xFF438883)];
final onboardingGradient = [Color(0xFFE5F6F5), Color(0xFFCEEEEC)];
final profileGradient = [Color(0xFF2D9C9A), Color(0xFF43B4B1)];
final splashGradient = [Color(0xFF4C9D97), Color(0xFF0C4C48)];

// Extra Special Colors
final balanceCardGreenColor = Color(0xFF448B86);
final scaffoldColor = Color(0xFF6AB7A8);
